# Cloud-Resource-Optimizer
Backend service that connects to a cloud environment, analyzes running resources, and uses an LLM to suggest right-sizing and identify zombie resources. Generates a Terraform plan to modify the infrastructure and stores each recommendation in a PostgreSQL database.
